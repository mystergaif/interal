# Website Auditor

Простой кроссплатформенный инструмент для базового аудита веб-сайтов, разработанный на JavaFX.

## Функциональность

*   Базовый поиск уязвимостей (SQL-инъекции, XSS).
*   Идентификация используемых библиотек и фреймворков (базовая).
*   Графический интерфейс в стиле терминала.

## Сборка и запуск

Для сборки и запуска проекта вам потребуется установлен Maven и Java 11 или выше.

1.  Клонируйте репозиторий:
    ```bash
    git clone [URL вашего репозитория]
    ```
2.  Перейдите в директорию проекта:
    ```bash
    cd website-auditor
    ```
3.  Соберите проект с помощью Maven:
    ```bash
    mvn clean package
    ```
    Эта команда соберет исполняемый JAR-файл в директории `target`.
4.  Запустите приложение:
    ```bash
    java -jar target/website-auditor-1.0-SNAPSHOT.jar
    ```

## TODO

*   Расширить функциональность поиска уязвимостей (CSRF, другие типы).
*   Реализовать более точную идентификацию библиотек и их версий.
*   Интегрировать базу данных уязвимостей для проверки устаревших компонентов.
*   Улучшить отчетность.
*   Добавить возможность сканирования форм.
